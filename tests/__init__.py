"""Test package for elevenlabs-cli.

Integration tests are scripts under this package. They are networked and
require environment configuration (tokens). Run them only when explicitly
enabled in your environment.
"""

